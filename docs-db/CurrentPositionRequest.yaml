id: CurrentPositionRequest
source_file: background-geolocation-types/src/core/api/CurrentPositionRequest.ts
signature: export interface CurrentPositionRequest {
categories:
- Primary API
description: |-
  Options provided to {@link BackgroundGeolocation.getCurrentPosition}.

  @example example-1
examples:
  example-1:
    title: Example 1
    code:
      ts: |-
        let location = await BackgroundGeolocation.getCurrentPosition({
          timeout: 30,          // 30 second timeout to fetch location
          persist: true,        // Defaults to state.enabled
          maximumAge: 5000,     // Accept the last-known-location if not older than 5000 ms.
          desiredAccuracy: 10,  // Try to fetch a location with an accuracy of `10` meters.
          samples: 3,           // How many location samples to attempt.
          extras: {             // Custom meta-data.
            "route_id": 123
          }
        });
