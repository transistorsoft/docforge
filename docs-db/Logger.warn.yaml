id: Logger.warn
source_file: background-geolocation-types/src/core/api/Logger.ts
signature: 'warn(message: string): void;'
description: |-
  Inserts a "warning" log message into the SDK's log database

  @example example-1
  &nbsp;

  ```
  E TSLocationManager: [c.t.l.logger.TSLog log]
  E TSLocationManager: ⚠️  Something WEIRD
  ```
examples:
  example-1:
    title: Example 1
    code:
      ts: BackgroundGeolocation.logger.warn("Something WEIRD");
