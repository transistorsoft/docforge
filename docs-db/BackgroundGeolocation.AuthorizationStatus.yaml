id: BackgroundGeolocation.AuthorizationStatus
source_file: background-geolocation-types/src/core/api/BackgroundGeolocation.ts
signature: 'AuthorizationStatus: typeof import(''../../enums/AuthorizationStatus'').AuthorizationStatus;'
description: |-
  __AuthorizationStatus__
  Represents OS-level authorization state for location-services
  (Denied, Restricted, Always, WhenInUse).
  Returned from requestPermission() and onProviderChange.

  @example example-1
  @readonly
examples:
  example-1:
    title: Example 1
    code:
      ts: |-
        const status = await BackgroundGeolocation.requestPermission();
        if (status === BackgroundGeolocation.AuthorizationStatus.Always) {
          // Good to start tracking
        }
