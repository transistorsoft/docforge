id: BackgroundGeolocation.destroyLocation
source_file: background-geolocation-types/src/core/api/BackgroundGeolocation.ts
signature: 'destroyLocation(uuid: string): Promise<void>;'
description: |-
  Destroy a single location by {@link Location.uuid}

  @example example-1
examples:
  example-1:
    title: Example 1
    code:
      ts: await BackgroundGeolocation.destroyLocation(location.uuid);
