id: BackgroundGeolocation.destroyLocations
source_file: background-geolocation-types/src/core/api/BackgroundGeolocation.ts
signature: 'destroyLocations(): Promise<void>;'
description: |-
  Remove all records in SDK's SQLite database.

  @example example-1
examples:
  example-1:
    title: Example 1
    code:
      ts: let success = await BackgroundGeolocation.destroyLocations();
