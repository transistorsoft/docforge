id: TransistorAuthorizationToken
source_file: background-geolocation-types/src/core/api/TransistorAuthorizationService.ts
signature: export interface TransistorAuthorizationToken {
categories:
- Demo / Debug Server
description: |-
  Represents an authorization token issued by a Transistorsoft Tracking Server.

  Returned from {@link TransistorAuthorizationService.findOrCreate} and consumed by
  `Config.authorization` / `transistorAuthorizationToken` flows.
