id: BackgroundGeolocation.geofenceExists
source_file: background-geolocation-types/src/core/api/BackgroundGeolocation.ts
signature: 'geofenceExists(identifier: string): Promise<boolean>;'
description: |-
  Determine if a particular geofence exists in the SDK's database.

  @example example-1
  __ℹ️ See also:__
  - 📘 {@link Geofence | Geofencing Guide}
examples:
  example-1:
    title: Example 1
    code:
      ts: |-
        let exists = await BackgroundGeolocation.geofenceExists("HOME");
        console.log("[geofenceExists] ", exists);
