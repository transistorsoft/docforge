id: BackgroundGeolocation.getLocations
source_file: background-geolocation-types/src/core/api/BackgroundGeolocation.ts
signature: 'getLocations(): Promise<Array<Object>>;'
description: |-
  Retrieve a List of {@link Location} currently stored in the SDK's SQLite database.

  @example example-1
examples:
  example-1:
    title: Example 1
    code:
      ts: let locations = await BackgroundGeolocation.getLocations();
