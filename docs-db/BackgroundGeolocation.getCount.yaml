id: BackgroundGeolocation.getCount
source_file: background-geolocation-types/src/core/api/BackgroundGeolocation.ts
signature: 'getCount(): Promise<number>;'
description: |-
  Retrieve the count of all locations current stored in the SDK's SQLite database.

  @example example-1
examples:
  example-1:
    title: Example 1
    code:
      ts: let count = await BackgroundGeolocation.getCount();
